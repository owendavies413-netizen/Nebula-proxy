/* scram/scramjet.config.js */
self.__scramjet_config = {
    prefix: '/service/',
    // This line links to the variable in the code you just showed me
    codec: self.__scramjet$codecs.xor, 
    config: 'config',
    cache: 'cache'
};